name: CI Smoke Test

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "Runner user:"
          whoami
          echo "Working directory:"
          pwd
          echo "CI läuft auf der Cloud-VM 🎉"


